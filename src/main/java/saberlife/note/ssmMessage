框架搭建的时候遇到很多有有意思的问题，日志配置写的是绝对路径写到d盘 log文件夹下

mybatis 配置：classpath*:saberlife/mappingxml/*.xml   classpath 后不加* 会出现问题
classpath 是一个定位资源路径的入口
    classpath和classpath* 的区别
    classpath：只会到你的class路径中查找文件
    classpath*：不仅包含class路径，还包括jar文件中的（class路径）进行查找